'use strict';
const shebangRegex = require('shebang-regex');

module.exports = (string = '') => ***REMOVED***
	const match = string.match(shebangRegex);

	if (!match) ***REMOVED***
		return null;
	***REMOVED***

	const [path, argument] = match[0].replace(/#! ?/, '').split(' ');
	const binary = path.split('/').pop();

	if (binary === 'env') ***REMOVED***
		return argument;
	***REMOVED***

	return argument ? `$***REMOVED***binary***REMOVED*** $***REMOVED***argument***REMOVED***` : binary;
***REMOVED***;
