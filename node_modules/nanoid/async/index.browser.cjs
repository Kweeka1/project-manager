let random = async bytes => crypto.getRandomValues(new Uint8Array(bytes))
let customAlphabet = (alphabet, defaultSize = 21) => ***REMOVED***
  let mask = (2 << (Math.log(alphabet.length - 1) / Math.LN2)) - 1
  let step = -~((1.6 * mask * defaultSize) / alphabet.length)
  return async (size = defaultSize) => ***REMOVED***
    let id = ''
    while (true) ***REMOVED***
      let bytes = crypto.getRandomValues(new Uint8Array(step))
      let i = step
      while (i--) ***REMOVED***
        id += alphabet[bytes[i] & mask] || ''
        if (id.length === size) return id
  ***REMOVED***
***REMOVED***
***REMOVED***
***REMOVED***
let nanoid = async (size = 21) => ***REMOVED***
  let id = ''
  let bytes = crypto.getRandomValues(new Uint8Array(size))
  while (size--) ***REMOVED***
    let byte = bytes[size] & 63
    if (byte < 36) ***REMOVED***
      id += byte.toString(36)
***REMOVED*** else if (byte < 62) ***REMOVED***
      id += (byte - 26).toString(36).toUpperCase()
***REMOVED*** else if (byte < 63) ***REMOVED***
      id += '_'
***REMOVED*** else ***REMOVED***
      id += '-'
***REMOVED***
***REMOVED***
  return id
***REMOVED***
module.exports = ***REMOVED*** nanoid, customAlphabet, random ***REMOVED***
