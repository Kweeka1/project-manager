#!/usr/bin/env node

let ***REMOVED*** nanoid, customAlphabet ***REMOVED*** = require('..')

function print(msg) ***REMOVED***
  process.stdout.write(msg + '\n')
***REMOVED***

function error(msg) ***REMOVED***
  process.stderr.write(msg + '\n')
  process.exit(1)
***REMOVED***

if (process.argv.includes('--help') || process.argv.includes('-h')) ***REMOVED***
  print(`
  Usage
    $ nanoid [options]

  Options
    -s, --size       Generated ID size
    -a, --alphabet   Alphabet to use
    -h, --help       Show this help

  Examples
    $ nanoid --s 15
    S9sBF77U6sDB8Yg

    $ nanoid --size 10 --alphabet abc
    bcabababca`)
  process.exit()
***REMOVED***

let alphabet, size
for (let i = 2; i < process.argv.length; i++) ***REMOVED***
  let arg = process.argv[i]
  if (arg === '--size' || arg === '-s') ***REMOVED***
    size = Number(process.argv[i + 1])
    i += 1
    if (Number.isNaN(size) || size <= 0) ***REMOVED***
      error('Size must be positive integer')
***REMOVED***
***REMOVED*** else if (arg === '--alphabet' || arg === '-a') ***REMOVED***
    alphabet = process.argv[i + 1]
    i += 1
***REMOVED*** else ***REMOVED***
    error('Unknown argument ' + arg)
***REMOVED***
***REMOVED***

if (alphabet) ***REMOVED***
  let customNanoid = customAlphabet(alphabet, size)
  print(customNanoid())
***REMOVED*** else ***REMOVED***
  print(nanoid(size))
***REMOVED***
