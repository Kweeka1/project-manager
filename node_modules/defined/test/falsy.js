var test = require('tape');
var defined = require('../');

test('falsy', function (t) ***REMOVED***
    t.plan(1);
    var opts = ***REMOVED*** y : false, w : 4 ***REMOVED***;
    var x = defined(opts.x, opts.y, opts.w, 8);
    t.equal(x, false);
***REMOVED***);
