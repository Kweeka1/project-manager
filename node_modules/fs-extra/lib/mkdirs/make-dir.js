'use strict'
const fs = require('../fs')
const ***REMOVED*** checkPath ***REMOVED*** = require('./utils')

const getMode = options => ***REMOVED***
  const defaults = ***REMOVED*** mode: 0o777 ***REMOVED***
  if (typeof options === 'number') return options
  return (***REMOVED*** ...defaults, ...options ***REMOVED***).mode
***REMOVED***

module.exports.makeDir = async (dir, options) => ***REMOVED***
  checkPath(dir)

  return fs.mkdir(dir, ***REMOVED***
    mode: getMode(options),
    recursive: true
***REMOVED***)
***REMOVED***

module.exports.makeDirSync = (dir, options) => ***REMOVED***
  checkPath(dir)

  return fs.mkdirSync(dir, ***REMOVED***
    mode: getMode(options),
    recursive: true
***REMOVED***)
***REMOVED***
