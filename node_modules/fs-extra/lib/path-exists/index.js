'use strict'
const u = require('universalify').fromPromise
const fs = require('../fs')

function pathExists (path) ***REMOVED***
  return fs.access(path).then(() => true).catch(() => false)
***REMOVED***

module.exports = ***REMOVED***
  pathExists: u(pathExists),
  pathExistsSync: fs.existsSync
***REMOVED***
