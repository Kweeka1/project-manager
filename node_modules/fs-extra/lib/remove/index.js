'use strict'

const fs = require('graceful-fs')
const u = require('universalify').fromCallback
const rimraf = require('./rimraf')

function remove (path, callback) ***REMOVED***
  // Node 14.14.0+
  if (fs.rm) return fs.rm(path, ***REMOVED*** recursive: true, force: true ***REMOVED***, callback)
  rimraf(path, callback)
***REMOVED***

function removeSync (path) ***REMOVED***
  // Node 14.14.0+
  if (fs.rmSync) return fs.rmSync(path, ***REMOVED*** recursive: true, force: true ***REMOVED***)
  rimraf.sync(path)
***REMOVED***

module.exports = ***REMOVED***
  remove: u(remove),
  removeSync
***REMOVED***
