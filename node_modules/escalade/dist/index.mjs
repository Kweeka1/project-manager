import ***REMOVED*** dirname, resolve ***REMOVED*** from 'path';
import ***REMOVED*** readdir, stat ***REMOVED*** from 'fs';
import ***REMOVED*** promisify ***REMOVED*** from 'util';

const toStats = promisify(stat);
const toRead = promisify(readdir);

export default async function (start, callback) ***REMOVED***
	let dir = resolve('.', start);
	let tmp, stats = await toStats(dir);

	if (!stats.isDirectory()) ***REMOVED***
		dir = dirname(dir);
	***REMOVED***

	while (true) ***REMOVED***
		tmp = await callback(dir, await toRead(dir));
		if (tmp) return resolve(dir, tmp);
		dir = dirname(tmp = dir);
		if (tmp === dir) break;
	***REMOVED***
***REMOVED***
