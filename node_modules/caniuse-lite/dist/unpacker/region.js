'use strict'

const browsers = require('./browsers').browsers

function unpackRegion(packed) ***REMOVED***
  return Object.keys(packed).reduce((list, browser) => ***REMOVED***
    let data = packed[browser]
    list[browsers[browser]] = Object.keys(data).reduce((memo, key) => ***REMOVED***
      let stats = data[key]
      if (key === '_') ***REMOVED***
        stats.split(' ').forEach(version => (memo[version] = null))
  ***REMOVED*** else ***REMOVED***
        memo[key] = stats
  ***REMOVED***
      return memo
***REMOVED***, ***REMOVED******REMOVED***)
    return list
***REMOVED*** ***REMOVED******REMOVED***)
***REMOVED***

module.exports = unpackRegion
module.exports.default = unpackRegion
