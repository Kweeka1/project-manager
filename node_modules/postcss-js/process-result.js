let objectify = require('./objectifier')

module.exports = function processResult(result) ***REMOVED***
  if (console && console.warn) ***REMOVED***
    result.warnings().forEach(warn => ***REMOVED***
      let source = warn.plugin || 'PostCSS'
      console.warn(source + ': ' + warn.text)
***REMOVED***)
***REMOVED***
  return objectify(result.root)
***REMOVED***
