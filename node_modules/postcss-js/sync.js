let postcss = require('postcss')

let processResult = require('./process-result')
let parse = require('./parser')

module.exports = function (plugins) ***REMOVED***
  let processor = postcss(plugins)
  return input => ***REMOVED***
    let result = processor.process(input, ***REMOVED*** parser: parse, from: undefined ***REMOVED***)
    return processResult(result)
***REMOVED***
***REMOVED***
