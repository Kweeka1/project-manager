let postcss = require('postcss')

let processResult = require('./process-result')
let parse = require('./parser')

module.exports = function async(plugins) ***REMOVED***
  let processor = postcss(plugins)
  return async input => ***REMOVED***
    let result = await processor.process(input, ***REMOVED***
      parser: parse,
      from: undefined
***REMOVED***)
    return processResult(result)
***REMOVED***
***REMOVED***
