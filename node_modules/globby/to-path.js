import ***REMOVED***fileURLToPath***REMOVED*** from 'node:url';

const toPath = urlOrPath => ***REMOVED***
	if (!urlOrPath) ***REMOVED***
		return urlOrPath;
	***REMOVED***

	if (urlOrPath instanceof URL) ***REMOVED***
		urlOrPath = urlOrPath.href;
	***REMOVED***

	return urlOrPath.startsWith('file://') ? fileURLToPath(urlOrPath) : urlOrPath;
***REMOVED***;

export default toPath;
