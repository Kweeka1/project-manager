#!/usr/bin/env pwsh
$basedir=Split-Path $MyInvocation.MyCommand.Definition -Parent

$exe=""
if ($PSVersionTable.PSVersion -lt "6.0" -or $IsWindows) ***REMOVED***
  # Fix case when both the Windows and Linux builds of Node
  # are installed in the same directory
  $exe=".exe"
***REMOVED***
$ret=0
if (Test-Path "$basedir/node$exe") ***REMOVED***
  & "$basedir/node$exe"  "$basedir/../cross-env/src/bin/cross-env.js" $args
  $ret=$LASTEXITCODE
***REMOVED*** else ***REMOVED***
  & "node$exe"  "$basedir/../cross-env/src/bin/cross-env.js" $args
  $ret=$LASTEXITCODE
***REMOVED***
exit $ret
