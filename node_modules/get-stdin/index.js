const ***REMOVED***stdin***REMOVED*** = process;

export default async function getStdin() ***REMOVED***
	let result = '';

	if (stdin.isTTY) ***REMOVED***
		return result;
	***REMOVED***

	stdin.setEncoding('utf8');

	for await (const chunk of stdin) ***REMOVED***
		result += chunk;
	***REMOVED***

	return result;
***REMOVED***

getStdin.buffer = async () => ***REMOVED***
	const result = [];
	let length = 0;

	if (stdin.isTTY) ***REMOVED***
		return Buffer.concat([]);
	***REMOVED***

	for await (const chunk of stdin) ***REMOVED***
		result.push(chunk);
		length += chunk.length;
	***REMOVED***

	return Buffer.concat(result, length);
***REMOVED***;
