/**
 * @license
 * Copyright (c) 2016, Contributors
 * SPDX-License-Identifier: ISC
 */
// take an un-split argv string and tokenize it.
export function tokenizeArgString(argString) ***REMOVED***
    if (Array.isArray(argString)) ***REMOVED***
        return argString.map(e => typeof e !== 'string' ? e + '' : e);
***REMOVED***
    argString = argString.trim();
    let i = 0;
    let prevC = null;
    let c = null;
    let opening = null;
    const args = [];
    for (let ii = 0; ii < argString.length; ii++) ***REMOVED***
        prevC = c;
        c = argString.charAt(ii);
        // split on spaces unless we're in quotes.
        if (c === ' ' && !opening) ***REMOVED***
            if (!(prevC === ' ')) ***REMOVED***
                i++;
        ***REMOVED***
            continue;
    ***REMOVED***
        // don't split the string if we're in matching
        // opening or closing single and double quotes.
        if (c === opening) ***REMOVED***
            opening = null;
    ***REMOVED***
        else if ((c === "'" || c === '"') && !opening) ***REMOVED***
            opening = c;
    ***REMOVED***
        if (!args[i])
            args[i] = '';
        args[i] += c;
***REMOVED***
    return args;
***REMOVED***
