'use strict'

/* eslint-disable no-var */

var test = require('tape')
var buildQueue = require('../')

test('concurrency', function (t) ***REMOVED***
  t.plan(2)
  t.throws(buildQueue.bind(null, worker, 0))
  t.doesNotThrow(buildQueue.bind(null, worker, 1))

  function worker (arg, cb) ***REMOVED***
    cb(null, true)
***REMOVED***
***REMOVED***)

test('worker execution', function (t) ***REMOVED***
  t.plan(3)

  var queue = buildQueue(worker, 1)

  queue.push(42, function (err, result) ***REMOVED***
    t.error(err, 'no error')
    t.equal(result, true, 'result matches')
***REMOVED***)

  function worker (arg, cb) ***REMOVED***
    t.equal(arg, 42)
    cb(null, true)
***REMOVED***
***REMOVED***)

test('limit', function (t) ***REMOVED***
  t.plan(4)

  var expected = [10, 0]
  var queue = buildQueue(worker, 1)

  queue.push(10, result)
  queue.push(0, result)

  function result (err, arg) ***REMOVED***
    t.error(err, 'no error')
    t.equal(arg, expected.shift(), 'the result matches')
***REMOVED***

  function worker (arg, cb) ***REMOVED***
    setTimeout(cb, arg, null, arg)
***REMOVED***
***REMOVED***)

test('multiple executions', function (t) ***REMOVED***
  t.plan(15)

  var queue = buildQueue(worker, 1)
  var toExec = [1, 2, 3, 4, 5]
  var count = 0

  toExec.forEach(function (task) ***REMOVED***
    queue.push(task, done)
***REMOVED***)

  function done (err, result) ***REMOVED***
    t.error(err, 'no error')
    t.equal(result, toExec[count - 1], 'the result matches')
***REMOVED***

  function worker (arg, cb) ***REMOVED***
    t.equal(arg, toExec[count], 'arg matches')
    count++
    setImmediate(cb, null, arg)
***REMOVED***
***REMOVED***)

test('multiple executions, one after another', function (t) ***REMOVED***
  t.plan(15)

  var queue = buildQueue(worker, 1)
  var toExec = [1, 2, 3, 4, 5]
  var count = 0

  queue.push(toExec[0], done)

  function done (err, result) ***REMOVED***
    t.error(err, 'no error')
    t.equal(result, toExec[count - 1], 'the result matches')
    if (count < toExec.length) ***REMOVED***
      queue.push(toExec[count], done)
***REMOVED***
***REMOVED***

  function worker (arg, cb) ***REMOVED***
    t.equal(arg, toExec[count], 'arg matches')
    count++
    setImmediate(cb, null, arg)
***REMOVED***
***REMOVED***)

test('set this', function (t) ***REMOVED***
  t.plan(3)

  var that = ***REMOVED******REMOVED***
  var queue = buildQueue(that, worker, 1)

  queue.push(42, function (err, result) ***REMOVED***
    t.error(err, 'no error')
    t.equal(this, that, 'this matches')
***REMOVED***)

  function worker (arg, cb) ***REMOVED***
    t.equal(this, that, 'this matches')
    cb(null, true)
***REMOVED***
***REMOVED***)

test('drain', function (t) ***REMOVED***
  t.plan(4)

  var queue = buildQueue(worker, 1)
  var worked = false

  queue.push(42, function (err, result) ***REMOVED***
    t.error(err, 'no error')
    t.equal(result, true, 'result matches')
***REMOVED***)

  queue.drain = function () ***REMOVED***
    t.equal(true, worked, 'drained')
***REMOVED***

  function worker (arg, cb) ***REMOVED***
    t.equal(arg, 42)
    worked = true
    setImmediate(cb, null, true)
***REMOVED***
***REMOVED***)

test('pause && resume', function (t) ***REMOVED***
  t.plan(7)

  var queue = buildQueue(worker, 1)
  var worked = false

  t.notOk(queue.paused, 'it should not be paused')

  queue.pause()

  queue.push(42, function (err, result) ***REMOVED***
    t.error(err, 'no error')
    t.equal(result, true, 'result matches')
***REMOVED***)

  t.notOk(worked, 'it should be paused')
  t.ok(queue.paused, 'it should be paused')

  queue.resume()
  queue.resume() // second resume is a no-op

  t.notOk(queue.paused, 'it should not be paused')

  function worker (arg, cb) ***REMOVED***
    t.equal(arg, 42)
    worked = true
    cb(null, true)
***REMOVED***
***REMOVED***)

test('pause in flight && resume', function (t) ***REMOVED***
  t.plan(9)

  var queue = buildQueue(worker, 1)
  var expected = [42, 24]

  t.notOk(queue.paused, 'it should not be paused')

  queue.push(42, function (err, result) ***REMOVED***
    t.error(err, 'no error')
    t.equal(result, true, 'result matches')
    t.ok(queue.paused, 'it should be paused')
    process.nextTick(function () ***REMOVED*** queue.resume() ***REMOVED***)
***REMOVED***)

  queue.push(24, function (err, result) ***REMOVED***
    t.error(err, 'no error')
    t.equal(result, true, 'result matches')
    t.notOk(queue.paused, 'it should not be paused')
***REMOVED***)

  queue.pause()

  function worker (arg, cb) ***REMOVED***
    t.equal(arg, expected.shift())
    process.nextTick(function () ***REMOVED*** cb(null, true) ***REMOVED***)
***REMOVED***
***REMOVED***)

test('altering concurrency', function (t) ***REMOVED***
  t.plan(7)

  var queue = buildQueue(worker, 1)
  var count = 0

  queue.pause()

  queue.push(24, workDone)
  queue.push(24, workDone)

  queue.concurrency = 2

  queue.resume()

  t.equal(queue.running(), 2, '2 jobs running')

  function workDone (err, result) ***REMOVED***
    t.error(err, 'no error')
    t.equal(result, true, 'result matches')
***REMOVED***

  function worker (arg, cb) ***REMOVED***
    t.equal(0, count, 'works in parallel')
    setImmediate(function () ***REMOVED***
      count++
      cb(null, true)
***REMOVED***)
***REMOVED***
***REMOVED***)

test('idle()', function (t) ***REMOVED***
  t.plan(12)

  var queue = buildQueue(worker, 1)

  t.ok(queue.idle(), 'queue is idle')

  queue.push(42, function (err, result) ***REMOVED***
    t.error(err, 'no error')
    t.equal(result, true, 'result matches')
    t.notOk(queue.idle(), 'queue is not idle')
***REMOVED***)

  queue.push(42, function (err, result) ***REMOVED***
    t.error(err, 'no error')
    t.equal(result, true, 'result matches')
    // it will go idle after executing this function
    setImmediate(function () ***REMOVED***
      t.ok(queue.idle(), 'queue is now idle')
***REMOVED***)
***REMOVED***)

  t.notOk(queue.idle(), 'queue is not idle')

  function worker (arg, cb) ***REMOVED***
    t.notOk(queue.idle(), 'queue is not idle')
    t.equal(arg, 42)
    setImmediate(cb, null, true)
***REMOVED***
***REMOVED***)

test('saturated', function (t) ***REMOVED***
  t.plan(9)

  var queue = buildQueue(worker, 1)
  var preworked = 0
  var worked = 0

  queue.saturated = function () ***REMOVED***
    t.pass('saturated')
    t.equal(preworked, 1, 'started 1 task')
    t.equal(worked, 0, 'worked zero task')
***REMOVED***

  queue.push(42, done)
  queue.push(42, done)

  function done (err, result) ***REMOVED***
    t.error(err, 'no error')
    t.equal(result, true, 'result matches')
***REMOVED***

  function worker (arg, cb) ***REMOVED***
    t.equal(arg, 42)
    preworked++
    setImmediate(function () ***REMOVED***
      worked++
      cb(null, true)
***REMOVED***)
***REMOVED***
***REMOVED***)

test('length', function (t) ***REMOVED***
  t.plan(7)

  var queue = buildQueue(worker, 1)

  t.equal(queue.length(), 0, 'nothing waiting')
  queue.push(42, done)
  t.equal(queue.length(), 0, 'nothing waiting')
  queue.push(42, done)
  t.equal(queue.length(), 1, 'one task waiting')
  queue.push(42, done)
  t.equal(queue.length(), 2, 'two tasks waiting')

  function done (err, result) ***REMOVED***
    t.error(err, 'no error')
***REMOVED***

  function worker (arg, cb) ***REMOVED***
    setImmediate(function () ***REMOVED***
      cb(null, true)
***REMOVED***)
***REMOVED***
***REMOVED***)

test('getQueue', function (t) ***REMOVED***
  t.plan(10)

  var queue = buildQueue(worker, 1)

  t.equal(queue.getQueue().length, 0, 'nothing waiting')
  queue.push(42, done)
  t.equal(queue.getQueue().length, 0, 'nothing waiting')
  queue.push(42, done)
  t.equal(queue.getQueue().length, 1, 'one task waiting')
  t.equal(queue.getQueue()[0], 42, 'should be equal')
  queue.push(43, done)
  t.equal(queue.getQueue().length, 2, 'two tasks waiting')
  t.equal(queue.getQueue()[0], 42, 'should be equal')
  t.equal(queue.getQueue()[1], 43, 'should be equal')

  function done (err, result) ***REMOVED***
    t.error(err, 'no error')
***REMOVED***

  function worker (arg, cb) ***REMOVED***
    setImmediate(function () ***REMOVED***
      cb(null, true)
***REMOVED***)
***REMOVED***
***REMOVED***)

test('unshift', function (t) ***REMOVED***
  t.plan(8)

  var queue = buildQueue(worker, 1)
  var expected = [1, 2, 3, 4]

  queue.push(1, done)
  queue.push(4, done)
  queue.unshift(3, done)
  queue.unshift(2, done)

  function done (err, result) ***REMOVED***
    t.error(err, 'no error')
***REMOVED***

  function worker (arg, cb) ***REMOVED***
    t.equal(expected.shift(), arg, 'tasks come in order')
    setImmediate(function () ***REMOVED***
      cb(null, true)
***REMOVED***)
***REMOVED***
***REMOVED***)

test('unshift && empty', function (t) ***REMOVED***
  t.plan(2)

  var queue = buildQueue(worker, 1)
  var completed = false

  queue.pause()

  queue.empty = function () ***REMOVED***
    t.notOk(completed, 'the task has not completed yet')
***REMOVED***

  queue.unshift(1, done)

  queue.resume()

  function done (err, result) ***REMOVED***
    completed = true
    t.error(err, 'no error')
***REMOVED***

  function worker (arg, cb) ***REMOVED***
    setImmediate(function () ***REMOVED***
      cb(null, true)
***REMOVED***)
***REMOVED***
***REMOVED***)

test('push && empty', function (t) ***REMOVED***
  t.plan(2)

  var queue = buildQueue(worker, 1)
  var completed = false

  queue.pause()

  queue.empty = function () ***REMOVED***
    t.notOk(completed, 'the task has not completed yet')
***REMOVED***

  queue.push(1, done)

  queue.resume()

  function done (err, result) ***REMOVED***
    completed = true
    t.error(err, 'no error')
***REMOVED***

  function worker (arg, cb) ***REMOVED***
    setImmediate(function () ***REMOVED***
      cb(null, true)
***REMOVED***)
***REMOVED***
***REMOVED***)

test('kill', function (t) ***REMOVED***
  t.plan(5)

  var queue = buildQueue(worker, 1)
  var expected = [1]

  var predrain = queue.drain

  queue.drain = function drain () ***REMOVED***
    t.fail('drain should never be called')
***REMOVED***

  queue.push(1, done)
  queue.push(4, done)
  queue.unshift(3, done)
  queue.unshift(2, done)
  queue.kill()

  function done (err, result) ***REMOVED***
    t.error(err, 'no error')
    setImmediate(function () ***REMOVED***
      t.equal(queue.length(), 0, 'no queued tasks')
      t.equal(queue.running(), 0, 'no running tasks')
      t.equal(queue.drain, predrain, 'drain is back to default')
***REMOVED***)
***REMOVED***

  function worker (arg, cb) ***REMOVED***
    t.equal(expected.shift(), arg, 'tasks come in order')
    setImmediate(function () ***REMOVED***
      cb(null, true)
***REMOVED***)
***REMOVED***
***REMOVED***)

test('killAndDrain', function (t) ***REMOVED***
  t.plan(6)

  var queue = buildQueue(worker, 1)
  var expected = [1]

  var predrain = queue.drain

  queue.drain = function drain () ***REMOVED***
    t.pass('drain has been called')
***REMOVED***

  queue.push(1, done)
  queue.push(4, done)
  queue.unshift(3, done)
  queue.unshift(2, done)
  queue.killAndDrain()

  function done (err, result) ***REMOVED***
    t.error(err, 'no error')
    setImmediate(function () ***REMOVED***
      t.equal(queue.length(), 0, 'no queued tasks')
      t.equal(queue.running(), 0, 'no running tasks')
      t.equal(queue.drain, predrain, 'drain is back to default')
***REMOVED***)
***REMOVED***

  function worker (arg, cb) ***REMOVED***
    t.equal(expected.shift(), arg, 'tasks come in order')
    setImmediate(function () ***REMOVED***
      cb(null, true)
***REMOVED***)
***REMOVED***
***REMOVED***)

test('pause && idle', function (t) ***REMOVED***
  t.plan(11)

  var queue = buildQueue(worker, 1)
  var worked = false

  t.notOk(queue.paused, 'it should not be paused')
  t.ok(queue.idle(), 'should be idle')

  queue.pause()

  queue.push(42, function (err, result) ***REMOVED***
    t.error(err, 'no error')
    t.equal(result, true, 'result matches')
***REMOVED***)

  t.notOk(worked, 'it should be paused')
  t.ok(queue.paused, 'it should be paused')
  t.notOk(queue.idle(), 'should not be idle')

  queue.resume()

  t.notOk(queue.paused, 'it should not be paused')
  t.notOk(queue.idle(), 'it should not be idle')

  function worker (arg, cb) ***REMOVED***
    t.equal(arg, 42)
    worked = true
    process.nextTick(cb.bind(null, null, true))
    process.nextTick(function () ***REMOVED***
      t.ok(queue.idle(), 'is should be idle')
***REMOVED***)
***REMOVED***
***REMOVED***)

test('push without cb', function (t) ***REMOVED***
  t.plan(1)

  var queue = buildQueue(worker, 1)

  queue.push(42)

  function worker (arg, cb) ***REMOVED***
    t.equal(arg, 42)
    cb()
***REMOVED***
***REMOVED***)

test('unshift without cb', function (t) ***REMOVED***
  t.plan(1)

  var queue = buildQueue(worker, 1)

  queue.unshift(42)

  function worker (arg, cb) ***REMOVED***
    t.equal(arg, 42)
    cb()
***REMOVED***
***REMOVED***)

test('push with worker throwing error', function (t) ***REMOVED***
  t.plan(5)
  var q = buildQueue(function (task, cb) ***REMOVED***
    cb(new Error('test error'), null)
***REMOVED*** 1)
  q.error(function (err, task) ***REMOVED***
    t.ok(err instanceof Error, 'global error handler should catch the error')
    t.match(err.message, /test error/, 'error message should be "test error"')
    t.equal(task, 42, 'The task executed should be passed')
***REMOVED***)
  q.push(42, function (err) ***REMOVED***
    t.ok(err instanceof Error, 'push callback should catch the error')
    t.match(err.message, /test error/, 'error message should be "test error"')
***REMOVED***)
***REMOVED***)
