"use strict";

var pattern = /-(\w|$)/g;

var callback = function callback(dashChar, char) ***REMOVED***
	return char.toUpperCase();
***REMOVED***;

var camelCaseCSS = function camelCaseCSS(property) ***REMOVED***
	property = property.toLowerCase();

	// NOTE :: IE8's "styleFloat" is intentionally not supported
	if (property === "float") ***REMOVED***
		return "cssFloat";
	***REMOVED***
	// Microsoft vendor-prefixes are uniquely cased
	else if (property.charCodeAt(0) === 45&& property.charCodeAt(1) === 109&& property.charCodeAt(2) === 115&& property.charCodeAt(3) === 45) ***REMOVED***
			return property.substr(1).replace(pattern, callback);
		***REMOVED*** else ***REMOVED***
			return property.replace(pattern, callback);
		***REMOVED***
***REMOVED***;

module.exports = camelCaseCSS;
