import postcss from 'postcss'
import cloneNodes from './cloneNodes'

export default function responsive(rules) ***REMOVED***
  return postcss
    .atRule(***REMOVED***
      name: 'responsive',
***REMOVED***)
    .append(cloneNodes(Array.isArray(rules) ? rules : [rules]))
***REMOVED***
