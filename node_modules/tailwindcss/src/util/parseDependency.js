import isGlob from 'is-glob'
import globParent from 'glob-parent'
import path from 'path'

// Based on `glob-base`
// https://github.com/micromatch/glob-base/blob/master/index.js
function parseGlob(pattern) ***REMOVED***
  let glob = pattern
  let base = globParent(pattern)

  if (base !== '.') ***REMOVED***
    glob = pattern.substr(base.length)
    if (glob.charAt(0) === '/') ***REMOVED***
      glob = glob.substr(1)
***REMOVED***
***REMOVED***

  if (glob.substr(0, 2) === './') ***REMOVED***
    glob = glob.substr(2)
***REMOVED***
  if (glob.charAt(0) === '/') ***REMOVED***
    glob = glob.substr(1)
***REMOVED***

  return ***REMOVED*** base, glob ***REMOVED***
***REMOVED***

export default function parseDependency(normalizedFileOrGlob) ***REMOVED***
  if (normalizedFileOrGlob.startsWith('!')) ***REMOVED***
    return null
***REMOVED***

  let message

  if (isGlob(normalizedFileOrGlob)) ***REMOVED***
    let ***REMOVED*** base, glob ***REMOVED*** = parseGlob(normalizedFileOrGlob)
    message = ***REMOVED*** type: 'dir-dependency', dir: path.resolve(base), glob ***REMOVED***
***REMOVED*** else ***REMOVED***
    message = ***REMOVED*** type: 'dependency', file: path.resolve(normalizedFileOrGlob) ***REMOVED***
***REMOVED***

  // rollup-plugin-postcss does not support dir-dependency messages
  // but directories can be watched in the same way as files
  if (message.type === 'dir-dependency' && process.env.ROLLUP_WATCH === 'true') ***REMOVED***
    message = ***REMOVED*** type: 'dependency', file: message.dir ***REMOVED***
***REMOVED***

  return message
***REMOVED***
