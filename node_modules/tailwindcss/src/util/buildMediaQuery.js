export default function buildMediaQuery(screens) ***REMOVED***
  screens = Array.isArray(screens) ? screens : [screens]

  return screens
    .map((screen) =>
      screen.values.map((screen) => ***REMOVED***
        if (screen.raw !== undefined) ***REMOVED***
          return screen.raw
    ***REMOVED***

        return [
          screen.min && `(min-width: $***REMOVED***screen.min***REMOVED***)`,
          screen.max && `(max-width: $***REMOVED***screen.max***REMOVED***)`,
        ]
          .filter(Boolean)
          .join(' and ')
  ***REMOVED***)
    )
    .join(', ')
***REMOVED***
