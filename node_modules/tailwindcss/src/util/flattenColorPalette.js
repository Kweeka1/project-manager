const flattenColorPalette = (colors) =>
  Object.assign(
    ***REMOVED******REMOVED***,
    ...Object.entries(colors ?? ***REMOVED******REMOVED***).flatMap(([color, values]) =>
      typeof values == 'object'
        ? Object.entries(flattenColorPalette(values)).map(([number, hex]) => (***REMOVED***
            [color + (number === 'DEFAULT' ? '' : `-$***REMOVED***number***REMOVED***`)]: hex,
      ***REMOVED***))
        : [***REMOVED*** [`$***REMOVED***color***REMOVED***`]: values ***REMOVED***]
    )
  )

export default flattenColorPalette
