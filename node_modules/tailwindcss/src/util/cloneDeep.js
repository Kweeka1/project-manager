export function cloneDeep(value) ***REMOVED***
  if (Array.isArray(value)) ***REMOVED***
    return value.map((child) => cloneDeep(child))
***REMOVED***

  if (typeof value === 'object' && value !== null) ***REMOVED***
    return Object.fromEntries(Object.entries(value).map(([k, v]) => [k, cloneDeep(v)]))
***REMOVED***

  return value
***REMOVED***
