"use strict";
Object.defineProperty(exports, "__esModule", ***REMOVED***
    value: true
***REMOVED***);
exports.formatVariantSelector = formatVariantSelector;
exports.finalizeSelector = finalizeSelector;
exports.selectorFunctions = void 0;
var _postcssSelectorParser = _interopRequireDefault(require("postcss-selector-parser"));
var _unesc = _interopRequireDefault(require("postcss-selector-parser/dist/util/unesc"));
var _escapeClassName = _interopRequireDefault(require("../util/escapeClassName"));
var _prefixSelector = _interopRequireDefault(require("../util/prefixSelector"));
function _interopRequireDefault(obj) ***REMOVED***
    return obj && obj.__esModule ? obj : ***REMOVED***
        default: obj
***REMOVED***;
***REMOVED***
let MERGE = ":merge";
let PARENT = "&";
let selectorFunctions = new Set([
    MERGE
]);
exports.selectorFunctions = selectorFunctions;
function formatVariantSelector(current, ...others) ***REMOVED***
    for (let other of others)***REMOVED***
        let incomingValue = resolveFunctionArgument(other, MERGE);
        if (incomingValue !== null) ***REMOVED***
            let existingValue = resolveFunctionArgument(current, MERGE, incomingValue);
            if (existingValue !== null) ***REMOVED***
                let existingTarget = `$***REMOVED***MERGE***REMOVED***($***REMOVED***incomingValue***REMOVED***)`;
                let splitIdx = other.indexOf(existingTarget);
                let addition = other.slice(splitIdx + existingTarget.length).split(" ")[0];
                current = current.replace(existingTarget, existingTarget + addition);
                continue;
        ***REMOVED***
    ***REMOVED***
        current = other.replace(PARENT, current);
***REMOVED***
    return current;
***REMOVED***
function finalizeSelector(format, ***REMOVED*** selector: selector1 , candidate , context***REMOVED***) ***REMOVED***
    var ref, ref1;
    var ref2;
    let separator = (ref2 = context === null || context === void 0 ? void 0 : (ref = context.tailwindConfig) === null || ref === void 0 ? void 0 : ref.separator) !== null && ref2 !== void 0 ? ref2 : ":";
    // Split by the separator, but ignore the separator inside square brackets:
    //
    // E.g.: dark:lg:hover:[paint-order:markers]
    //           ┬  ┬     ┬            ┬
    //           │  │     │            ╰── We will not split here
    //           ╰──┴─────┴─────────────── We will split here
    //
    let splitter = new RegExp(`\\$***REMOVED***separator***REMOVED***(?![^[]*\\])`);
    let base = candidate.split(splitter).pop();
    if (context === null || context === void 0 ? void 0 : (ref1 = context.tailwindConfig) === null || ref1 === void 0 ? void 0 : ref1.prefix) ***REMOVED***
        format = (0, _prefixSelector).default(context.tailwindConfig.prefix, format);
***REMOVED***
    format = format.replace(PARENT, `.$***REMOVED***(0, _escapeClassName).default(candidate)***REMOVED***`);
    // Normalize escaped classes, e.g.:
    //
    // The idea would be to replace the escaped `base` in the selector with the
    // `format`. However, in css you can escape the same selector in a few
    // different ways. This would result in different strings and therefore we
    // can't replace it properly.
    //
    //               base: bg-[rgb(255,0,0)]
    //   base in selector: bg-\\[rgb\\(255\\,0\\,0\\)\\]
    //       escaped base: bg-\\[rgb\\(255\\2c 0\\2c 0\\)\\]
    //
    selector1 = (0, _postcssSelectorParser).default((selectors)=>***REMOVED***
        return selectors.walkClasses((node)=>***REMOVED***
            if (node.raws && node.value.includes(base)) ***REMOVED***
                node.raws.value = (0, _escapeClassName).default((0, _unesc).default(node.raws.value));
        ***REMOVED***
            return node;
    ***REMOVED***);
***REMOVED***).processSync(selector1);
    // We can safely replace the escaped base now, since the `base` section is
    // now in a normalized escaped value.
    selector1 = selector1.replace(`.$***REMOVED***(0, _escapeClassName).default(base)***REMOVED***`, format);
    // Remove unnecessary pseudo selectors that we used as placeholders
    return (0, _postcssSelectorParser).default((selectors)=>***REMOVED***
        return selectors.map((selector2)=>***REMOVED***
            selector2.walkPseudos((p)=>***REMOVED***
                if (selectorFunctions.has(p.value)) ***REMOVED***
                    p.replaceWith(p.nodes);
            ***REMOVED***
                return p;
        ***REMOVED***);
            // This will make sure to move pseudo's to the correct spot (the end for
            // pseudo elements) because otherwise the selector will never work
            // anyway.
            //
            // E.g.:
            //  - `before:hover:text-center` would result in `.before\:hover\:text-center:hover::before`
            //  - `hover:before:text-center` would result in `.hover\:before\:text-center:hover::before`
            //
            // `::before:hover` doesn't work, which means that we can make it work for you by flipping the order.
            function collectPseudoElements(selector) ***REMOVED***
                let nodes = [];
                for (let node of selector.nodes)***REMOVED***
                    if (isPseudoElement(node)) ***REMOVED***
                        nodes.push(node);
                        selector.removeChild(node);
                ***REMOVED***
                    if (node === null || node === void 0 ? void 0 : node.nodes) ***REMOVED***
                        nodes.push(...collectPseudoElements(node));
                ***REMOVED***
            ***REMOVED***
                return nodes;
        ***REMOVED***
            let pseudoElements = collectPseudoElements(selector2);
            if (pseudoElements.length > 0) ***REMOVED***
                selector2.nodes.push(pseudoElements.sort(sortSelector));
        ***REMOVED***
            return selector2;
    ***REMOVED***);
***REMOVED***).processSync(selector1);
***REMOVED***
// Note: As a rule, double colons (::) should be used instead of a single colon
// (:). This distinguishes pseudo-classes from pseudo-elements. However, since
// this distinction was not present in older versions of the W3C spec, most
// browsers support both syntaxes for the original pseudo-elements.
let pseudoElementsBC = [
    ":before",
    ":after",
    ":first-line",
    ":first-letter"
];
// These pseudo-elements _can_ be combined with other pseudo selectors AND the order does matter.
let pseudoElementExceptions = [
    "::file-selector-button"
];
// This will make sure to move pseudo's to the correct spot (the end for
// pseudo elements) because otherwise the selector will never work
// anyway.
//
// E.g.:
//  - `before:hover:text-center` would result in `.before\:hover\:text-center:hover::before`
//  - `hover:before:text-center` would result in `.hover\:before\:text-center:hover::before`
//
// `::before:hover` doesn't work, which means that we can make it work
// for you by flipping the order.
function sortSelector(a, z) ***REMOVED***
    // Both nodes are non-pseudo's so we can safely ignore them and keep
    // them in the same order.
    if (a.type !== "pseudo" && z.type !== "pseudo") ***REMOVED***
        return 0;
***REMOVED***
    // If one of them is a combinator, we need to keep it in the same order
    // because that means it will start a new "section" in the selector.
    if (a.type === "combinator" ^ z.type === "combinator") ***REMOVED***
        return 0;
***REMOVED***
    // One of the items is a pseudo and the other one isn't. Let's move
    // the pseudo to the right.
    if (a.type === "pseudo" ^ z.type === "pseudo") ***REMOVED***
        return (a.type === "pseudo") - (z.type === "pseudo");
***REMOVED***
    // Both are pseudo's, move the pseudo elements (except for
    // ::file-selector-button) to the right.
    return isPseudoElement(a) - isPseudoElement(z);
***REMOVED***
function isPseudoElement(node) ***REMOVED***
    if (node.type !== "pseudo") return false;
    if (pseudoElementExceptions.includes(node.value)) return false;
    return node.value.startsWith("::") || pseudoElementsBC.includes(node.value);
***REMOVED***
function resolveFunctionArgument(haystack, needle, arg) ***REMOVED***
    let startIdx = haystack.indexOf(arg ? `$***REMOVED***needle***REMOVED***($***REMOVED***arg***REMOVED***)` : needle);
    if (startIdx === -1) return null;
    // Start inside the `(`
    startIdx += needle.length + 1;
    let target = "";
    let count = 0;
    for (let char of haystack.slice(startIdx))***REMOVED***
        if (char !== "(" && char !== ")") ***REMOVED***
            target += char;
    ***REMOVED*** else if (char === "(") ***REMOVED***
            target += char;
            count++;
    ***REMOVED*** else if (char === ")") ***REMOVED***
            if (--count < 0) break; // unbalanced
            target += char;
    ***REMOVED***
***REMOVED***
    return target;
***REMOVED***
