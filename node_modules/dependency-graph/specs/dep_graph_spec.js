var dep_graph = require("../lib/dep_graph");
var DepGraph = dep_graph.DepGraph;

describe("DepGraph", function () ***REMOVED***
  it("should be able to add/remove nodes", function () ***REMOVED***
    var graph = new DepGraph();

    graph.addNode("Foo");
    graph.addNode("Bar");

    expect(graph.hasNode("Foo")).toBeTrue();
    expect(graph.hasNode("Bar")).toBeTrue();
    expect(graph.hasNode("NotThere")).toBeFalse();

    graph.removeNode("Bar");

    expect(graph.hasNode("Bar")).toBeFalse();
***REMOVED***);

  it("should calculate its size", function () ***REMOVED***
    var graph = new DepGraph();

    expect(graph.size()).toBe(0);

    graph.addNode("Foo");
    graph.addNode("Bar");

    expect(graph.size()).toBe(2);

    graph.removeNode("Bar");

    expect(graph.size()).toBe(1);
***REMOVED***);

  it("should treat the node data parameter as optional and use the node name as data if node data was not given", function () ***REMOVED***
    var graph = new DepGraph();

    graph.addNode("Foo");

    expect(graph.getNodeData("Foo")).toBe("Foo");
***REMOVED***);

  it("should be able to associate a node name with data on node add", function () ***REMOVED***
    var graph = new DepGraph();

    graph.addNode("Foo", "data");

    expect(graph.getNodeData("Foo")).toBe("data");
***REMOVED***);

  it("should be able to add undefined as node data", function () ***REMOVED***
    var graph = new DepGraph();

    graph.addNode("Foo", undefined);

    expect(graph.getNodeData("Foo")).toBeUndefined();
***REMOVED***);

  it("should return true when using hasNode with a node which has falsy data", function () ***REMOVED***
    var graph = new DepGraph();

    var falsyData = ["", 0, null, undefined, false];
    graph.addNode("Foo");

    falsyData.forEach(function (data) ***REMOVED***
      graph.setNodeData("Foo", data);

      expect(graph.hasNode("Foo")).toBeTrue();

      // Just an extra check to make sure that the saved data is correct
      expect(graph.getNodeData("Foo")).toBe(data);
***REMOVED***);
***REMOVED***);

  it("should be able to set data after a node was added", function () ***REMOVED***
    var graph = new DepGraph();

    graph.addNode("Foo", "data");
    graph.setNodeData("Foo", "data2");

    expect(graph.getNodeData("Foo")).toBe("data2");
***REMOVED***);

  it("should throw an error if we try to set data for a non-existing node", function () ***REMOVED***
    var graph = new DepGraph();

    expect(function () ***REMOVED***
      graph.setNodeData("Foo", "data");
***REMOVED***).toThrow(new Error("Node does not exist: Foo"));
***REMOVED***);

  it("should throw an error if the node does not exists and we try to get data", function () ***REMOVED***
    var graph = new DepGraph();

    expect(function () ***REMOVED***
      graph.getNodeData("Foo");
***REMOVED***).toThrow(new Error("Node does not exist: Foo"));
***REMOVED***);

  it("should do nothing if creating a node that already exists", function () ***REMOVED***
    var graph = new DepGraph();

    graph.addNode("a");
    graph.addNode("b");

    graph.addDependency("a", "b");

    graph.addNode("a");

    expect(graph.dependenciesOf("a")).toEqual(["b"]);
***REMOVED***);

  it("should do nothing if removing a node that does not exist", function () ***REMOVED***
    var graph = new DepGraph();

    graph.addNode("a");
    expect(graph.hasNode("a")).toBeTrue();

    graph.removeNode("a");
    expect(graph.hasNode("Foo")).toBeFalse();

    graph.removeNode("a");
    expect(graph.hasNode("Foo")).toBeFalse();
***REMOVED***);

  it("should be able to add dependencies between nodes", function () ***REMOVED***
    var graph = new DepGraph();

    graph.addNode("a");
    graph.addNode("b");
    graph.addNode("c");

    graph.addDependency("a", "b");
    graph.addDependency("a", "c");

    expect(graph.dependenciesOf("a")).toEqual(["b", "c"]);
***REMOVED***);

  it("should find entry nodes", function () ***REMOVED***
    var graph = new DepGraph();

    graph.addNode("a");
    graph.addNode("b");
    graph.addNode("c");

    graph.addDependency("a", "b");
    graph.addDependency("a", "c");

    expect(graph.entryNodes()).toEqual(["a"]);
***REMOVED***);

  it("should throw an error if a node does not exist and a dependency is added", function () ***REMOVED***
    var graph = new DepGraph();

    graph.addNode("a");

    expect(function () ***REMOVED***
      graph.addDependency("a", "b");
***REMOVED***).toThrow(new Error("Node does not exist: b"));
***REMOVED***);

  it("should detect cycles", function () ***REMOVED***
    var graph = new DepGraph();

    graph.addNode("a");
    graph.addNode("b");
    graph.addNode("c");
    graph.addNode("d");

    graph.addDependency("a", "b");
    graph.addDependency("b", "c");
    graph.addDependency("c", "a");
    graph.addDependency("d", "a");

    expect(function () ***REMOVED***
      graph.dependenciesOf("b");
***REMOVED***).toThrow(new dep_graph.DepGraphCycleError(["b", "c", "a", "b"]));
***REMOVED***);

  it("should allow cycles when configured", function () ***REMOVED***
    var graph = new DepGraph(***REMOVED*** circular: true ***REMOVED***);

    graph.addNode("a");
    graph.addNode("b");
    graph.addNode("c");
    graph.addNode("d");

    graph.addDependency("a", "b");
    graph.addDependency("b", "c");
    graph.addDependency("c", "a");
    graph.addDependency("d", "a");

    expect(graph.dependenciesOf("b")).toEqual(["a", "c"]);
    expect(graph.overallOrder()).toEqual(["c", "b", "a", "d"]);
***REMOVED***);

  it(
    "should include all nodes in overall order even from " +
    "cycles in disconnected subgraphs when circular is true",
    function () ***REMOVED***
      var graph = new DepGraph(***REMOVED*** circular: true ***REMOVED***);

      graph.addNode("2a");
      graph.addNode("2b");
      graph.addNode("2c");
      graph.addDependency("2a", "2b");
      graph.addDependency("2b", "2c");
      graph.addDependency("2c", "2a");

      graph.addNode("1a");
      graph.addNode("1b");
      graph.addNode("1c");
      graph.addNode("1d");
      graph.addNode("1e");

      graph.addDependency("1a", "1b");
      graph.addDependency("1a", "1c");
      graph.addDependency("1b", "1c");
      graph.addDependency("1c", "1d");

      expect(graph.overallOrder()).toEqual([
        "1d",
        "1c",
        "1b",
        "1a",
        "1e",
        "2c",
        "2b",
        "2a"
      ]);
***REMOVED***
  );

  it("should detect cycles in overall order", function () ***REMOVED***
    var graph = new DepGraph();

    graph.addNode("a");
    graph.addNode("b");
    graph.addNode("c");
    graph.addNode("d");

    graph.addDependency("a", "b");
    graph.addDependency("b", "c");
    graph.addDependency("c", "a");
    graph.addDependency("d", "a");

    expect(function () ***REMOVED***
      graph.overallOrder();
***REMOVED***).toThrow(new dep_graph.DepGraphCycleError(["a", "b", "c", "a"]));
***REMOVED***);

  it("should detect cycles in overall order when all nodes have dependants (incoming edges)", function () ***REMOVED***
    var graph = new DepGraph();

    graph.addNode("a");
    graph.addNode("b");
    graph.addNode("c");

    graph.addDependency("a", "b");
    graph.addDependency("b", "c");
    graph.addDependency("c", "a");

    expect(function () ***REMOVED***
      graph.overallOrder();
***REMOVED***).toThrow(new dep_graph.DepGraphCycleError(["a", "b", "c", "a"]));
***REMOVED***);

  it(
    "should detect cycles in overall order when there are several " +
    "disconnected subgraphs (with one that does not have a cycle",
    function () ***REMOVED***
      var graph = new DepGraph();

      graph.addNode("a_1");
      graph.addNode("a_2");
      graph.addNode("b_1");
      graph.addNode("b_2");
      graph.addNode("b_3");

      graph.addDependency("a_1", "a_2");
      graph.addDependency("b_1", "b_2");
      graph.addDependency("b_2", "b_3");
      graph.addDependency("b_3", "b_1");

      expect(function () ***REMOVED***
        graph.overallOrder();
  ***REMOVED***).toThrow(
        new dep_graph.DepGraphCycleError(["b_1", "b_2", "b_3", "b_1"])
      );
***REMOVED***
  );

  it("should retrieve dependencies and dependants in the correct order", function () ***REMOVED***
    var graph = new DepGraph();

    graph.addNode("a");
    graph.addNode("b");
    graph.addNode("c");
    graph.addNode("d");

    graph.addDependency("a", "d");
    graph.addDependency("a", "b");
    graph.addDependency("b", "c");
    graph.addDependency("d", "b");

    expect(graph.dependenciesOf("a")).toEqual(["c", "b", "d"]);
    expect(graph.dependenciesOf("b")).toEqual(["c"]);
    expect(graph.dependenciesOf("c")).toEqual([]);
    expect(graph.dependenciesOf("d")).toEqual(["c", "b"]);

    expect(graph.dependantsOf("a")).toEqual([]);
    expect(graph.dependantsOf("b")).toEqual(["a", "d"]);
    expect(graph.dependantsOf("c")).toEqual(["a", "d", "b"]);
    expect(graph.dependantsOf("d")).toEqual(["a"]);

    // check the alias "dependentsOf"
    expect(graph.dependentsOf("a")).toEqual([]);
    expect(graph.dependentsOf("b")).toEqual(["a", "d"]);
    expect(graph.dependentsOf("c")).toEqual(["a", "d", "b"]);
    expect(graph.dependentsOf("d")).toEqual(["a"]);
***REMOVED***);

  it("should be able to retrieve direct dependencies/dependants", function () ***REMOVED***
    var graph = new DepGraph();

    graph.addNode("a");
    graph.addNode("b");
    graph.addNode("c");
    graph.addNode("d");

    graph.addDependency("a", "d");
    graph.addDependency("a", "b");
    graph.addDependency("b", "c");
    graph.addDependency("d", "b");

    expect(graph.directDependenciesOf("a")).toEqual(["d", "b"]);
    expect(graph.directDependenciesOf("b")).toEqual(["c"]);
    expect(graph.directDependenciesOf("c")).toEqual([]);
    expect(graph.directDependenciesOf("d")).toEqual(["b"]);

    expect(graph.directDependantsOf("a")).toEqual([]);
    expect(graph.directDependantsOf("b")).toEqual(["a", "d"]);
    expect(graph.directDependantsOf("c")).toEqual(["b"]);
    expect(graph.directDependantsOf("d")).toEqual(["a"]);

    // check the alias "directDependentsOf"
    expect(graph.directDependentsOf("a")).toEqual([]);
    expect(graph.directDependentsOf("b")).toEqual(["a", "d"]);
    expect(graph.directDependentsOf("c")).toEqual(["b"]);
    expect(graph.directDependentsOf("d")).toEqual(["a"]);
***REMOVED***);

  it("should be able to resolve the overall order of things", function () ***REMOVED***
    var graph = new DepGraph();

    graph.addNode("a");
    graph.addNode("b");
    graph.addNode("c");
    graph.addNode("d");
    graph.addNode("e");

    graph.addDependency("a", "b");
    graph.addDependency("a", "c");
    graph.addDependency("b", "c");
    graph.addDependency("c", "d");

    expect(graph.overallOrder()).toEqual(["d", "c", "b", "a", "e"]);
***REMOVED***);

  it('should be able to only retrieve the "leaves" in the overall order', function () ***REMOVED***
    var graph = new DepGraph();

    graph.addNode("a");
    graph.addNode("b");
    graph.addNode("c");
    graph.addNode("d");
    graph.addNode("e");

    graph.addDependency("a", "b");
    graph.addDependency("a", "c");
    graph.addDependency("b", "c");
    graph.addDependency("c", "d");

    expect(graph.overallOrder(true)).toEqual(["d", "e"]);
***REMOVED***);

  it("should be able to give the overall order for a graph with several disconnected subgraphs", function () ***REMOVED***
    var graph = new DepGraph();

    graph.addNode("a_1");
    graph.addNode("a_2");
    graph.addNode("b_1");
    graph.addNode("b_2");
    graph.addNode("b_3");

    graph.addDependency("a_1", "a_2");
    graph.addDependency("b_1", "b_2");
    graph.addDependency("b_2", "b_3");

    expect(graph.overallOrder()).toEqual(["a_2", "a_1", "b_3", "b_2", "b_1"]);
***REMOVED***);

  it("should give an empty overall order for an empty graph", function () ***REMOVED***
    var graph = new DepGraph();

    expect(graph.overallOrder()).toEqual([]);
***REMOVED***);

  it("should still work after nodes are removed", function () ***REMOVED***
    var graph = new DepGraph();

    graph.addNode("a");
    graph.addNode("b");
    graph.addNode("c");
    graph.addDependency("a", "b");
    graph.addDependency("b", "c");

    expect(graph.dependenciesOf("a")).toEqual(["c", "b"]);

    graph.removeNode("c");

    expect(graph.dependenciesOf("a")).toEqual(["b"]);
***REMOVED***);

  it("should clone an empty graph", function () ***REMOVED***
    var graph = new DepGraph();
    expect(graph.size()).toEqual(0);
    var cloned = graph.clone();
    expect(cloned.size()).toEqual(0);

    expect(graph === cloned).toBeFalse();
***REMOVED***);

  it("should clone a non-empty graph", function () ***REMOVED***
    var graph = new DepGraph();

    graph.addNode("a");
    graph.addNode("b");
    graph.addNode("c");
    graph.addDependency("a", "b");
    graph.addDependency("b", "c");

    var cloned = graph.clone();

    expect(graph === cloned).toBeFalse();
    expect(cloned.hasNode("a")).toBeTrue();
    expect(cloned.hasNode("b")).toBeTrue();
    expect(cloned.hasNode("c")).toBeTrue();
    expect(cloned.dependenciesOf("a")).toEqual(["c", "b"]);
    expect(cloned.dependantsOf("c")).toEqual(["a", "b"]);

    // Changes to the original graph shouldn't affect the clone
    graph.removeNode("c");
    expect(graph.dependenciesOf("a")).toEqual(["b"]);
    expect(cloned.dependenciesOf("a")).toEqual(["c", "b"]);

    graph.addNode("d");
    graph.addDependency("b", "d");
    expect(graph.dependenciesOf("a")).toEqual(["d", "b"]);
    expect(cloned.dependenciesOf("a")).toEqual(["c", "b"]);
***REMOVED***);

  it("should only be a shallow clone", function () ***REMOVED***
    var graph = new DepGraph();

    var data = ***REMOVED*** a: 42 ***REMOVED***;
    graph.addNode("a", data);

    var cloned = graph.clone();
    expect(graph === cloned).toBeFalse();
    expect(graph.getNodeData("a") === cloned.getNodeData("a")).toBeTrue();

    graph.getNodeData("a").a = 43;
    expect(cloned.getNodeData("a").a).toBe(43);

    cloned.setNodeData("a", ***REMOVED*** a: 42 ***REMOVED***);
    expect(cloned.getNodeData("a").a).toBe(42);
    expect(graph.getNodeData("a") === cloned.getNodeData("a")).toBeFalse();
***REMOVED***);
***REMOVED***);

describe("DepGraph Performance", function () ***REMOVED***
  it("should not exceed max call stack with a very deep graph", function () ***REMOVED***
    var g = new DepGraph();
    var expected = [];
    for (var i = 0; i < 100000; i++) ***REMOVED***
      var istr = i.toString();
      g.addNode(istr);
      expected.push(istr);
      if (i > 0) ***REMOVED***
        g.addDependency(istr, (i - 1).toString());
  ***REMOVED***
***REMOVED***
    var order = g.overallOrder();
    expect(order).toEqual(expected);
***REMOVED***);

  it("should run an a reasonable amount of time for a very large graph", function () ***REMOVED***
    var randInt = function (min, max) ***REMOVED***
      return Math.floor(Math.random() * (max - min + 1)) + min;
***REMOVED***;
    var g = new DepGraph();
    var nodes = [];
    // Create a graph with 100000 nodes in it with 10 random connections to
    // lower numbered nodes
    for (var i = 0; i < 100000; i++) ***REMOVED***
      nodes.push(i.toString());
      g.addNode(i.toString());
      for (var j = 0; j < 10; j++) ***REMOVED***
        var dep = randInt(0, i);
        if (i !== dep) ***REMOVED***
          g.addDependency(i.toString(), dep.toString());
    ***REMOVED***
  ***REMOVED***
***REMOVED***
    var start = new Date().getTime();
    g.overallOrder();
    var end = new Date().getTime();
    expect(start - end).toBeLessThan(1000);
***REMOVED***);
***REMOVED***);

describe("DepGraphCycleError", function () ***REMOVED***
  var DepGraphCycleError = dep_graph.DepGraphCycleError;

  it("should have a message", function () ***REMOVED***
    var err = new DepGraphCycleError(["a", "b", "c", "a"]);
    expect(err.message).toEqual("Dependency Cycle Found: a -> b -> c -> a");
***REMOVED***);

  it("should be an instanceof DepGraphCycleError", function () ***REMOVED***
    var err = new DepGraphCycleError(["a", "b", "c", "a"]);
    expect(err instanceof DepGraphCycleError).toBeTrue();
    expect(err instanceof Error).toBeTrue();
***REMOVED***);

  it("should have a cyclePath", function () ***REMOVED***
    var cyclePath = ["a", "b", "c", "a"];
    var err = new DepGraphCycleError(cyclePath);
    expect(err.cyclePath).toEqual(cyclePath);
***REMOVED***);
***REMOVED***);
