declare namespace postcssValueParser ***REMOVED***
  interface BaseNode ***REMOVED***
    /**
     * The offset, inclusive, inside the CSS value at which the node starts.
     */
    sourceIndex: number;

    /**
     * The offset, exclusive, inside the CSS value at which the node ends.
     */
    sourceEndIndex: number;

    /**
     * The node's characteristic value
     */
    value: string;
***REMOVED***

  interface ClosableNode ***REMOVED***
    /**
     * Whether the parsed CSS value ended before the node was properly closed
     */
    unclosed?: true;
***REMOVED***

  interface AdjacentAwareNode ***REMOVED***
    /**
     * The token at the start of the node
     */
    before: string;

    /**
     * The token at the end of the node
     */
    after: string;
***REMOVED***

  interface CommentNode extends BaseNode, ClosableNode ***REMOVED***
    type: "comment";
***REMOVED***

  interface DivNode extends BaseNode, AdjacentAwareNode ***REMOVED***
    type: "div";
***REMOVED***

  interface FunctionNode extends BaseNode, ClosableNode, AdjacentAwareNode ***REMOVED***
    type: "function";

    /**
     * Nodes inside the function
     */
    nodes: Node[];
***REMOVED***

  interface SpaceNode extends BaseNode ***REMOVED***
    type: "space";
***REMOVED***

  interface StringNode extends BaseNode, ClosableNode ***REMOVED***
    type: "string";

    /**
     * The quote type delimiting the string
     */
    quote: '"' | "'";
***REMOVED***

  interface UnicodeRangeNode extends BaseNode ***REMOVED***
    type: "unicode-range";
***REMOVED***

  interface WordNode extends BaseNode ***REMOVED***
    type: "word";
***REMOVED***

  /**
   * Any node parsed from a CSS value
   */
  type Node =
    | CommentNode
    | DivNode
    | FunctionNode
    | SpaceNode
    | StringNode
    | UnicodeRangeNode
    | WordNode;

  interface CustomStringifierCallback ***REMOVED***
    /**
     * @param node The node to stringify
     * @returns The serialized CSS representation of the node
     */
    (nodes: Node): string | undefined;
***REMOVED***

  interface WalkCallback ***REMOVED***
    /**
     * @param node  The currently visited node
     * @param index The index of the node in the series of parsed nodes
     * @param nodes The series of parsed nodes
     * @returns Returning `false` will prevent traversal of descendant nodes (only applies if `bubble` was set to `true` in the `walk()` call)
     */
    (node: Node, index: number, nodes: Node[]): void | boolean;
***REMOVED***

  /**
   * A CSS dimension, decomposed into its numeric and unit parts
   */
  interface Dimension ***REMOVED***
    number: string;
    unit: string;
***REMOVED***

  /**
   * A wrapper around a parsed CSS value that allows for inspecting and walking nodes
   */
  interface ParsedValue ***REMOVED***
    /**
     * The series of parsed nodes
     */
    nodes: Node[];

    /**
     * Walk all parsed nodes, applying a callback
     *
     * @param callback A visitor callback that will be executed for each node
     * @param bubble   When set to `true`, walking will be done inside-out instead of outside-in
     */
    walk(callback: WalkCallback, bubble?: boolean): this;
***REMOVED***

  interface ValueParser ***REMOVED***
    /**
     * Decompose a CSS dimension into its numeric and unit part
     *
     * @param value The dimension to decompose
     * @returns An object representing `number` and `unit` part of the dimension or `false` if the decomposing fails
     */
    unit(value: string): Dimension | false;

    /**
     * Serialize a series of nodes into a CSS value
     *
     * @param nodes  The nodes to stringify
     * @param custom A custom stringifier callback
     * @returns The generated CSS value
     */
    stringify(nodes: Node | Node[], custom?: CustomStringifierCallback): string;

    /**
     * Walk a series of nodes, applying a callback
     *
     * @param nodes    The nodes to walk
     * @param callback A visitor callback that will be executed for each node
     * @param bubble   When set to `true`, walking will be done inside-out instead of outside-in
     */
    walk(nodes: Node[], callback: WalkCallback, bubble?: boolean): void;

    /**
     * Parse a CSS value into a series of nodes to operate on
     *
     * @param value The value to parse
     */
    new (value: string): ParsedValue;

    /**
     * Parse a CSS value into a series of nodes to operate on
     *
     * @param value The value to parse
     */
    (value: string): ParsedValue;
***REMOVED***
***REMOVED***

declare const postcssValueParser: postcssValueParser.ValueParser;

export = postcssValueParser;
