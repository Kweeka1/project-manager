module.exports = function walk(nodes, cb, bubble) ***REMOVED***
  var i, max, node, result;

  for (i = 0, max = nodes.length; i < max; i += 1) ***REMOVED***
    node = nodes[i];
    if (!bubble) ***REMOVED***
      result = cb(node, i, nodes);
***REMOVED***

    if (
      result !== false &&
      node.type === "function" &&
      Array.isArray(node.nodes)
    ) ***REMOVED***
      walk(node.nodes, cb, bubble);
***REMOVED***

    if (bubble) ***REMOVED***
      cb(node, i, nodes);
***REMOVED***
***REMOVED***
***REMOVED***;
