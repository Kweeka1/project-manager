import ***REMOVED***
  ProcessOptions,
  Plugin,
  SourceMap,
  TransformCallback,
  Root,
  Document,
  Node,
  Warning,
  WarningOptions
***REMOVED*** from './postcss.js'
import Processor from './processor.js'

export interface Message ***REMOVED***
  /**
   * Message type.
   */
  type: string

  /**
   * Source PostCSS plugin name.
   */
  plugin?: string

  [others: string]: any
***REMOVED***

export interface ResultOptions extends ProcessOptions ***REMOVED***
  /**
   * The CSS node that was the source of the warning.
   */
  node?: Node

  /**
   * Name of plugin that created this warning. `Result#warn` will fill it
   * automatically with `Plugin#postcssPlugin` value.
   */
  plugin?: string
***REMOVED***

/**
 * Provides the result of the PostCSS transformations.
 *
 * A Result instance is returned by `LazyResult#then`
 * or `Root#toResult` methods.
 *
 * ```js
 * postcss([autoprefixer]).process(css).then(result => ***REMOVED***
 *  console.log(result.css)
 * ***REMOVED***)
 * ```
 *
 * ```js
 * const result2 = postcss.parse(css).toResult()
 * ```
 */
export default class Result ***REMOVED***
  /**
   * The Processor instance used for this transformation.
   *
   * ```js
   * for (const plugin of result.processor.plugins) ***REMOVED***
   *   if (plugin.postcssPlugin === 'postcss-bad') ***REMOVED***
   *     throw 'postcss-good is incompatible with postcss-bad'
   * ***REMOVED***
   * ***REMOVED***)
   * ```
   */
  processor: Processor

  /**
   * Contains messages from plugins (e.g., warnings or custom messages).
   * Each message should have type and plugin properties.
   *
   * ```js
   * AtRule: ***REMOVED***
   *   import: (atRule, ***REMOVED*** result ***REMOVED***) ***REMOVED***
   *     const importedFile = parseImport(atRule)
   *     result.messages.push(***REMOVED***
   *       type: 'dependency',
   *       plugin: 'postcss-import',
   *       file: importedFile,
   *       parent: result.opts.from
   * ***REMOVED***)
   * ***REMOVED***
   * ***REMOVED***
   * ```
   */
  messages: Message[]

  /**
   * Root node after all transformations.
   *
   * ```js
   * root.toResult().root === root
   * ```
   */
  root: Root | Document

  /**
   * Options from the `Processor#process` or `Root#toResult` call
   * that produced this Result instance.]
   *
   * ```js
   * root.toResult(opts).opts === opts
   * ```
   */
  opts: ResultOptions

  /**
   * A CSS string representing of `Result#root`.
   *
   * ```js
   * postcss.parse('a***REMOVED******REMOVED***').toResult().css //=> "a***REMOVED******REMOVED***"
   * ```
   */
  css: string

  /**
   * An instance of `SourceMapGenerator` class from the `source-map` library,
   * representing changes to the `Result#root` instance.
   *
   * ```js
   * result.map.toJSON() //=> ***REMOVED*** version: 3, file: 'a.css', … ***REMOVED***
   * ```
   *
   * ```js
   * if (result.map) ***REMOVED***
   *   fs.writeFileSync(result.opts.to + '.map', result.map.toString())
   * ***REMOVED***
   * ```
   */
  map: SourceMap

  /**
   * Last runned PostCSS plugin.
   */
  lastPlugin: Plugin | TransformCallback

  /**
   * @param processor Processor used for this transformation.
   * @param root      Root node after all transformations.
   * @param opts      Options from the `Processor#process` or `Root#toResult`.
   */
  constructor(processor: Processor, root: Root | Document, opts: ResultOptions)

  /**
   * An alias for the `Result#css` property.
   * Use it with syntaxes that generate non-CSS output.
   *
   * ```js
   * result.css === result.content
   * ```
   */
  get content(): string

  /**
   * Returns for `Result#css` content.
   *
   * ```js
   * result + '' === result.css
   * ```
   *
   * @return String representing of `Result#root`.
   */
  toString(): string

  /**
   * Creates an instance of `Warning` and adds it to `Result#messages`.
   *
   * ```js
   * if (decl.important) ***REMOVED***
   *   result.warn('Avoid !important', ***REMOVED*** node: decl, word: '!important' ***REMOVED***)
   * ***REMOVED***
   * ```
   *
   * @param text Warning message.
   * @param opts Warning options.
   * @return Created warning.
   */
  warn(message: string, options?: WarningOptions): Warning

  /**
   * Returns warnings from plugins. Filters `Warning` instances
   * from `Result#messages`.
   *
   * ```js
   * result.warnings().forEach(warn => ***REMOVED***
   *   console.warn(warn.toString())
   * ***REMOVED***)
   * ```
   *
   * @return Warnings from plugins.
   */
  warnings(): Warning[]
***REMOVED***
