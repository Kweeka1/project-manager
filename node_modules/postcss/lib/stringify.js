'use strict'

let Stringifier = require('./stringifier')

function stringify(node, builder) ***REMOVED***
  let str = new Stringifier(builder)
  str.stringify(node)
***REMOVED***

module.exports = stringify
stringify.default = stringify
