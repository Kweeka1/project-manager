'use strict'

let Container = require('./container')

let LazyResult, Processor

class Document extends Container ***REMOVED***
  constructor(defaults) ***REMOVED***
    // type needs to be passed to super, otherwise child roots won't be normalized correctly
    super(***REMOVED*** type: 'document', ...defaults ***REMOVED***)

    if (!this.nodes) ***REMOVED***
      this.nodes = []
***REMOVED***
***REMOVED***

  toResult(opts = ***REMOVED******REMOVED***) ***REMOVED***
    let lazy = new LazyResult(new Processor(), this, opts)

    return lazy.stringify()
***REMOVED***
***REMOVED***

Document.registerLazyResult = dependant => ***REMOVED***
  LazyResult = dependant
***REMOVED***

Document.registerProcessor = dependant => ***REMOVED***
  Processor = dependant
***REMOVED***

module.exports = Document
Document.default = Document
