import Container, ***REMOVED*** ContainerProps ***REMOVED*** from './container.js'
import ***REMOVED*** ProcessOptions ***REMOVED*** from './postcss.js'
import Result from './result.js'
import Root, ***REMOVED*** RootProps ***REMOVED*** from './root.js'

export interface DocumentProps extends ContainerProps ***REMOVED***
  nodes?: Root[]

  /**
   * Information to generate byte-to-byte equal node string as it was
   * in the origin input.
   *
   * Every parser saves its own properties.
   */
  raws?: Record<string, any>
***REMOVED***

type ChildNode = Root
type ChildProps = RootProps

/**
 * Represents a file and contains all its parsed nodes.
 *
 * **Experimental:** some aspects of this node could change within minor
 * or patch version releases.
 *
 * ```js
 * const document = htmlParser(
 *   '<html><style>a***REMOVED***color:black***REMOVED***</style><style>b***REMOVED***z-index:2***REMOVED***</style>'
 * )
 * document.type         //=> 'document'
 * document.nodes.length //=> 2
 * ```
 */
export default class Document extends Container<Root> ***REMOVED***
  type: 'document'
  parent: undefined

  constructor(defaults?: DocumentProps)

  /**
   * Returns a `Result` instance representing the document’s CSS roots.
   *
   * ```js
   * const root1 = postcss.parse(css1, ***REMOVED*** from: 'a.css' ***REMOVED***)
   * const root2 = postcss.parse(css2, ***REMOVED*** from: 'b.css' ***REMOVED***)
   * const document = postcss.document()
   * document.append(root1)
   * document.append(root2)
   * const result = document.toResult(***REMOVED*** to: 'all.css', map: true ***REMOVED***)
   * ```
   *
   * @param opts Options.
   * @return Result with current document’s CSS.
   */
  toResult(options?: ProcessOptions): Result
***REMOVED***
