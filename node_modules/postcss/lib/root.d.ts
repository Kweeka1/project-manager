import Container, ***REMOVED*** ContainerProps ***REMOVED*** from './container.js'
import Document from './document.js'
import ***REMOVED*** ProcessOptions ***REMOVED*** from './postcss.js'
import Result from './result.js'

interface RootRaws extends Record<string, any> ***REMOVED***
  /**
   * The space symbols after the last child to the end of file.
   */
  after?: string

  /**
   * Non-CSS code before `Root`, when `Root` is inside `Document`.
   *
   * **Experimental:** some aspects of this node could change within minor
   * or patch version releases.
   */
  codeBefore?: string

  /**
   * Non-CSS code after `Root`, when `Root` is inside `Document`.
   *
   * **Experimental:** some aspects of this node could change within minor
   * or patch version releases.
   */
  codeAfter?: string

  /**
   * Is the last child has an (optional) semicolon.
   */
  semicolon?: boolean
***REMOVED***

export interface RootProps extends ContainerProps ***REMOVED***
  /**
   * Information used to generate byte-to-byte equal node string
   * as it was in the origin input.
   * */
  raws?: RootRaws
***REMOVED***

/**
 * Represents a CSS file and contains all its parsed nodes.
 *
 * ```js
 * const root = postcss.parse('a***REMOVED***color:black***REMOVED*** b***REMOVED***z-index:2***REMOVED***')
 * root.type         //=> 'root'
 * root.nodes.length //=> 2
 * ```
 */
export default class Root extends Container ***REMOVED***
  type: 'root'
  parent: Document | undefined
  raws: RootRaws

  /**
   * Returns a `Result` instance representing the root’s CSS.
   *
   * ```js
   * const root1 = postcss.parse(css1, ***REMOVED*** from: 'a.css' ***REMOVED***)
   * const root2 = postcss.parse(css2, ***REMOVED*** from: 'b.css' ***REMOVED***)
   * root1.append(root2)
   * const result = root1.toResult(***REMOVED*** to: 'all.css', map: true ***REMOVED***)
   * ```
   *
   * @param opts Options.
   * @return Result with current root’s CSS.
   */
  toResult(options?: ProcessOptions): Result

  constructor(defaults?: RootProps)
  assign(overrides: object | RootProps): this
***REMOVED***
