export default function dlv(obj, key, def, p, undef) ***REMOVED***
	key = key.split ? key.split('.') : key;
	for (p = 0; p < key.length; p++) ***REMOVED***
		obj = obj ? obj[key[p]] : undef;
	***REMOVED***
	return obj === undef ? def : obj;
***REMOVED***
